{
  "type": "module",
  "scripts": {
	"check": "bunx tsc --noEmit",
    "build": "forge build --force",
    "serve": "bun src/serve.ts",
	"test": "bun test --timeout 5000",
    "test-components": "bun test test/components/* --timeout 5000",
    "test-gateways": "bun test test/gateway/* --timeout 0"
  },
  "devDependencies": {
    "@adraffy/blocksmith": "^0.0.22",
    "@types/bun": "latest"
  },
  "dependencies": {
    "@resolverworks/ezccip": "^0.0.12",
    "ethers": "^6.13.1"
  },
  "peerDependencies": {
    "typescript": "^5.5.2"
  }
}
